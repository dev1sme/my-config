# Module manifest — read by install.sh and install.ps1
# Format: key=value, one per line, no inline comments
label=Docker Engine + Compose
hint=docker-ce, buildx, compose v2
order=30
mode=bg
default=on
sudo=linux
requires=
linux=setup.sh
mac=
windows=
next=User thường: logout/login lại để chạy docker không cần sudo
