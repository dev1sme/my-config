# Module manifest — read by install.sh and install.ps1
# Format: key=value, one per line, no inline comments
label=VS Code extensions + settings
hint=35 extensions + setting.json
order=40
mode=bg
default=on
sudo=
requires=code
linux=setup.sh
mac=setup_mac.sh
windows=setup.ps1
next=
