# Module manifest — read by install.sh and install.ps1
# Format: key=value, one per line, no inline comments
label=Zsh + Oh My Zsh
hint=plugins, fzf, theme strug
order=20
mode=tty
default=on
sudo=linux
requires=
linux=setup.sh
mac=setup_mac.sh
windows=
next=Chạy 'exec zsh' để dùng shell mới
